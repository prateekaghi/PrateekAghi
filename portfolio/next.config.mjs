/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/prateekaghi",
  assetPrefix: "/prateekaghi",
};

export default nextConfig;
